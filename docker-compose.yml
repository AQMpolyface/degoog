services:
  degoog:
    build: .
    environment:
      - DEGOOG_GLOBAL_AUTH=true
      - DEGOOG_PASSWORDS=Cuddle0-Sterilize0-Drift7-Eloquence1-Capacity8
    volumes:
      - ./data:/app/data
    ports:
      - "0.0.0.0:4444:4444"
    restart: unless-stopped
